<?xml version="1.0" encoding="UTF-8" ?>

<container xmlns="http://symfony.com/schema/dic/services"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xsi:schemaLocation="http://symfony.com/schema/dic/services http://symfony.com/schema/dic/services/services-1.0.xsd">
    <services>

        <!-- Autowire and autoconfigure in bundle is discouraged by the Symfony Core Team -->
        <!-- <defaults autowire="true" autoconfigure="true" public="false" /> -->

        <defaults public="false" />

        <!-- Controller declarations -->
        <service id="Acme\Bundle\Controller\AcmeController">
            <tag name="controller.service_arguments" />
            <tag name="container.service_subscriber" />
            <call method="setContainer"><argument type="service" id="Psr\Container\ContainerInterface" /></call>
            <argument type="service" id="acme.service" />
        </service>

        <!-- Listener declarations -->
        <service id="Acme\Bundle\Subscriber\AcmeListener">
            <tag name="kernel.event_listener" event="kernel.request" method="onKernelRequest" />
        </service>

        <!-- Subscriber declarations -->
        <service id="Acme\Bundle\Subscriber\AcmeSubscriber">
            <tag name="kernel.event_subscriber" />
        </service>

        <!-- Service declarations -->
        <service id="Acme\Bundle\Service\AcmeService" />

        <!-- Twig declarations -->
        <service id="Acme\Bundle\Twig\AcmeTwigExtension" >
            <tag name="twig.extension" />
        </service>

        <!-- Repository services -->
        <service id="Acme\Bundle\Repository\AnvilRepository">
           <tag name="doctrine.repository_service" />
           <argument type="service" id="doctrine" />
        </service>

    </services>
</container>
